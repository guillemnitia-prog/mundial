# Secretos — NUNCA versionar
.env
*.env
!.env.example
users_seed.*
!users_seed.example.json
*secret*
*.key

# Base de datos SQLite (la caché vive aquí; no se versiona)
*.db
*.sqlite
*.sqlite3
data/*.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Entornos virtuales
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# El modelo entrenado SÍ se versiona (se hornea en la imagen para el deploy gratis).
# (Regenerable con: python -m src.models.ensemble --train)
